{
  "name": "new",
  "version": "1.0.0",
  "description": "",
  "main": "rot-13.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src --watch -d build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  },
  "dependencies": {
    "nodemon": "^2.0.7"
  }
}
